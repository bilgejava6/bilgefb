const prod = 'https://www.benimsitem.com';
const dev = 'http://localhost';
const test = 'test.benimsitem.com';

const portLogin = ':9090';
const portRegister = ':9090';
const portHome = ':9090';

const version = '/api/v1';

const RestApi = {
    mainService: dev+portHome+version
};

export default RestApi;
